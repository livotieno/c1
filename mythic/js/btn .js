function buyNow(item) {
    alert('You have selected ' + item + '. Please proceed to checkout.');
    // You can replace the alert with a redirect to a checkout page or add-to-cart functionality.
    // For example, window.location.href = '/checkout?item=' + item;
}
